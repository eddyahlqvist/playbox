# dice.py

from ..system_commands import SystemCommand
from ..ui import info

class DiceToolbox:
    def run(self) -> SystemCommand | None:
        info("Dice, not yet implemented")
        return None