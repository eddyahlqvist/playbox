# calculator.py

from ..system_commands import SystemCommand
from ..ui import info

class CalculatorToolbox:
    def run(self) -> SystemCommand | None:
        info("Calculator, not yet implemented")
        return None