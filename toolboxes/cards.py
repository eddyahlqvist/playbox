# cards.py

from ..system_commands import SystemCommand
from ..ui import info

class CardsToolbox:
    def run(self) -> SystemCommand | None:
        info("Cards, not yet implemented")
        return None